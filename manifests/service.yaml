apiVersion: v1
kind: Service
metadata:
  name: fastapi-backend
  namespace: aug2025-poc
spec:
  selector:
    app: fastapi-backend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
